//http://poj.org/problem?id=1159
#include <bits/stdc++.h>
#define ll long long
using namespace std;
int main(){
	std::ios::sync_with_stdio(false);
	ll n;
	cin >> n;
	string s;
	cin >> s;
	vector<vector<ll>> dp(n, vector<ll>(n, -1));
	for(ll i = 1; i < n; i++){
		for(ll j = )
	}
}