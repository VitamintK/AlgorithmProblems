#include <bits/stdc++.h>
#include <string>

#define ll long long
#define ull unsigned long long
using namespace std;

int main(){
	std::ios::sync_with_stdio(false);
	int T;
	cin >> T;
	for(int t = 0; t < T; t++){
		int n, k;
		cin >> n >> k;
		//nvm just realized this problem requires numbers up to
		//50 fucking digits, so i'm going to switch to python.
		//why the fuck would they not accept the answer modulo 10^7.....
	}
	return 0;
}