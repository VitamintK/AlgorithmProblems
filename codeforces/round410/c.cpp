#include <bits/stdc++.h>
#include <string>

#define ll long long
#define ull unsigned long long
using namespace std;
int main(){
	std::ios::sync_with_stdio(false);
	int n;
	cin >> n;
	vector<ll> as(n);
	for(int i = 0; i < n; i++){
		ll a;
		cin >> a[i];
	}
	return 0;
}