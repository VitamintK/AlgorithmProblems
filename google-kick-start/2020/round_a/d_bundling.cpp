#include <map>
#include <set>
#include <list>
#include <cmath>
#include <ctime>
#include <deque>
#include <queue>
#include <stack>
#include <string>
#include <bitset>
#include <cstdio>
#include <limits>
#include <vector>
#include <climits>
#include <cstring>
#include <cstdlib>
#include <fstream>
#include <numeric>
#include <sstream>
#include <iostream>
#include <algorithm>
#include <set>
#include <queue>
#include <utility>
#define ll long long
using namespace std;

// thought I might need to use CPP for this but I realized I don't actually know how to make structs
// so I gave python a shot and it thankfully didn't TLE.  I should really get more practice with CPP though
// for when it does actually matter.


const int ALPHABET_SIZE = 26;

struct TrieNode {
    struct TrieNode *children
}

int main(){
    std::ios::sync_with_stdio(false);
    int T;
    cin >> T;
    for (int t = 0; t < T; t++){
        int n, k;
        cin >> n >> k;
        for(int i = 0; i < n; i++){

        }
        cout << "Case #" << t+1 << ": " << hi << endl;
    }
}