#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
#include <sstream>
#include <queue>
#include <deque>
#include <bitset>
#include <iterator>
#include <list>
#include <stack>
#include <map>
#include <set>
#include <functional>
#include <numeric>
#include <utility>
#include <limits>
using namespace std;
//inspired by anta's code
int main(){
	std::ios::sync_with_stdio(false);
	int n;
	cin >> n;
	vector<long> as(n);
	vector<vector<long>> seqs(n, -1);
	for(int i = 0; i < n; i++){
		cin >> as[i];
	}
	return 0;
}